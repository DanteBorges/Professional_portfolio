const TagsTecnology = (props ) => {
  return (
    <>
      <div className={`${props.color} font-bold text-lg rounded-full px-4  mr-2  py-1`}>
        <p>{props.name}</p>
      </div>
    </>
  );
};

export default TagsTecnology;
